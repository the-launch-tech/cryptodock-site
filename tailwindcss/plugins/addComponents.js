module.exports = function({ addComponents }) {
  addComponents({
    '.max-content': {
      maxWidth: 1170,
      margin: 'auto',
    },
    '.max-content-sm': {
      maxWidth: 900,
      margin: 'auto',
    },
    '.max-content-lg': {
      maxWidth: 1340,
      margin: 'auto',
    },
  })
}
